{
  "name": "zarobank_cypress_test_automation",
  "version": "1.0.0",
  "description": "Zero Bank Component and E2E testing",
  "main": "index.js",
  "scripts": {
    "cypress:runner": "cypress open",
    "cypress:execution": "cypress run",
    "runOnChrome": "cypress run browser --chrome",
    "cypress:AllureReport": "cypress run --browser chrome --env allure=true",
    "allure:report": "allure generate allure-results --clean -o allure-report"
  },
  "keywords": [
    "BDD",
    "Cucumber"
  ],
  "author": "nyamat allah",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^14.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "@shelex/cypress-allure-plugin": "^2.33.1",
    "cypress": "^11.0.1",
    "cypress-esbuild-preprocessor": "^1.0.2",
    "esbuild": "^0.15.13",
    "node-xlsx": "^0.21.0",
    "xlsx": "^0.18.5"
  }
}
